[build]
builder = "RAILPACK"
buildCommand = "npm ci && node scripts/write-environment.mjs && npm run build"
watchPatterns = ["src/**", "public/**", "scripts/**", "package.json", "package-lock.json", "angular.json", "server.mjs", "railway.toml"]

[deploy]
startCommand = "npm start"
healthcheckPath = "/healthz"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
